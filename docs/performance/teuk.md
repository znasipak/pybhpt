# `pybhpt.teuk` performance

Timing of the inhomogeneous Teukolsky amplitude solve. See the
[Performance overview](index) for the methodology and [reference machine](reference-machine).

```{note}
Benchmarks for the full `TeukolskyMode` amplitude solve — aggregate per-mode cost across
the parameter space plus a per-stage breakdown (radial solve, spheroidal harmonic, source
integration) — are in progress and will be added here once the corresponding data files
are committed under `benchmarks/data/`.
```
